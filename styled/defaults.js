import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  max-width: 1400px;
  padding: 0 1rem;
  margin-right: 0;
  margin-left: 0;
`;

export const Column = style.div`
    width: ${props}
`;
